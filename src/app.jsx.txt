import { useState } from "react";
import { BarChart, Bar, XAxis, YAxis, Tooltip, ResponsiveContainer, Cell } from "recharts";

const LPU_GOLD = "#F5A623";
const LPU_NAVY = "#1A2657";
const LPU_LIGHT = "#F0F4FF";

const sdgs = [
  { id: 1, name: "No Poverty", icon: "🏚️", score: 60, color: "#E5243B" },
  { id: 2, name: "Zero Hunger", icon: "🌾", score: 52, color: "#DDA63A" },
  { id: 3, name: "Good Health", icon: "💊", score: 58, color: "#4C9F38" },
  { id: 4, name: "Quality Education", icon: "📚", score: 64, color: "#C5192D" },
  { id: 5, name: "Gender Equality", icon: "♀️", score: 48, color: "#FF3A21" },
  { id: 6, name: "Clean Water", icon: "💧", score: 67, color: "#26BDE2" },
  { id: 7, name: "Clean Energy", icon: "⚡", score: 71, color: "#FCC30B" },
  { id: 8, name: "Decent Work", icon: "💼", score: 55, color: "#A21942" },
  { id: 9, name: "Industry & Innovation", icon: "🏭", score: 50, color: "#FD6925" },
  { id: 10, name: "Reduced Inequalities", icon: "⚖️", score: 43, color: "#DD1367" },
  { id: 11, name: "Sustainable Cities", icon: "🏙️", score: 46, color: "#FD9D24" },
  { id: 12, name: "Responsible Consumption", icon: "♻️", score: 54, color: "#BF8B2E" },
  { id: 13, name: "Climate Action", icon: "🌍", score: 62, color: "#3F7E44" },
  { id: 14, name: "Life Below Water", icon: "🐟", score: 41, color: "#0A97D9" },
  { id: 15, name: "Life on Land", icon: "🌳", score: 49, color: "#56C02B" },
  { id: 16, name: "Peace & Justice", icon: "🕊️", score: 57, color: "#00689D" },
  { id: 17, name: "Partnerships", icon: "🤝", score: 69, color: "#19486A" },
];

const milestones = [
  { date: "Jan 2025", event: "India launches Green Hydrogen Mission Phase II" },
  { date: "Mar 2025", event: "National SDG Report 2025 submitted to NITI Aayog" },
  { date: "Jun 2025", event: "Swachh Bharat Urban 2.0 mid-term review" },
  { date: "Sep 2025", event: "UN SDG Summit — India progress presentation" },
  { date: "Dec 2025", event: "India's Net Zero Roadmap checkpoint review" },
  { date: "Mar 2026", event: "Jal Jeevan Mission 100% rural coverage target" },
  { date: "Dec 2026", event: "PM-KISAN expanded coverage milestone" },
  { date: "2030", event: "Global SDG deadline — India's final review" },
];

const avgScore = Math.round(sdgs.reduce((a, b) => a + b.score, 0) / sdgs.length);
const topSDG = [...sdgs].sort((a, b) => b.score - a.score)[0];
const bottomSDG = [...sdgs].sort((a, b) => a.score - b.score)[0];
const getScoreColor = (s) => s >= 65 ? "#4C9F38" : s >= 50 ? LPU_GOLD : "#E5243B";

export default function App() {
  const [activeTab, setActiveTab] = useState("overview");
  return (
    <div style={{ background: LPU_LIGHT, minHeight: "100vh", fontFamily: "'Segoe UI', sans-serif" }}>
      <div style={{ background: LPU_NAVY, padding: "16px 24px", display: "flex", alignItems: "center", justifyContent: "space-between", boxShadow: "0 2px 12px rgba(0,0,0,0.3)" }}>
        <div style={{ display: "flex", alignItems: "center", gap: 14 }}>
          <div style={{ background: LPU_GOLD, borderRadius: 10, width: 46, height: 46, display: "flex", alignItems: "center", justifyContent: "center", fontWeight: 900, fontSize: 18, color: LPU_NAVY }}>LPU</div>
          <div>
            <div style={{ color: LPU_GOLD, fontWeight: 800, fontSize: 17 }}>Lovely Professional University</div>
            <div style={{ color: "#aab4d4", fontSize: 12 }}>SDG Progress Dashboard — India 2025</div>
          </div>
        </div>
        <div style={{ background: "#F5A62322", border: `1px solid ${LPU_GOLD}`, borderRadius: 20, padding: "4px 14px", color: LPU_GOLD, fontSize: 13, fontWeight: 600 }}>🌱 Sustainability Tracker</div>
      </div>

      <div style={{ background: LPU_NAVY, display: "flex", gap: 4, padding: "0 24px" }}>
        {["overview", "goals", "timeline"].map(tab => (
          <button key={tab} onClick={() => setActiveTab(tab)} style={{ background: activeTab === tab ? LPU_GOLD : "transparent", color: activeTab === tab ? LPU_NAVY : "#aab4d4", border: "none", padding: "10px 20px", borderRadius: "8px 8px 0 0", fontWeight: 700, fontSize: 13, cursor: "pointer", textTransform: "capitalize" }}>
            {tab === "overview" ? "📊 Overview" : tab === "goals" ? "🎯 All Goals" : "📅 Timeline"}
          </button>
        ))}
      </div>

      <div style={{ padding: "20px 24px", maxWidth: 1000, margin: "0 auto" }}>
        {activeTab === "overview" && (
          <>
            <div style={{ display: "grid", gridTemplateColumns: "repeat(3, 1fr)", gap: 16, marginBottom: 20 }}>
              {[
                { label: "Avg SDG Score", value: `${avgScore}/100`, sub: "India 2025 estimate", color: LPU_GOLD, icon: "📈" },
                { label: "Top Performing", value: topSDG.icon + " " + topSDG.name, sub: `Score: ${topSDG.score}`, color: "#4C9F38", icon: "🏆" },
                { label: "Needs Focus", value: bottomSDG.icon + " " + bottomSDG.name, sub: `Score: ${bottomSDG.score}`, color: "#E5243B", icon: "⚠️" },
              ].map((c, i) => (
                <div key={i} style={{ background: "#fff", borderRadius: 14, padding: 18, borderTop: `4px solid ${c.color}`, boxShadow: "0 2px 8px rgba(0,0,0,0.07)" }}>
                  <div style={{ fontSize: 22 }}>{c.icon}</div>
                  <div style={{ fontSize: 13, color: "#888", marginTop: 4 }}>{c.label}</div>
                  <div style={{ fontWeight: 800, fontSize: 15, color: LPU_NAVY, marginTop: 4 }}>{c.value}</div>
                  <div style={{ fontSize: 11, color: "#aaa", marginTop: 2 }}>{c.sub}</div>
                </div>
              ))}
            </div>
            <div style={{ background: "#fff", borderRadius: 14, padding: 20, boxShadow: "0 2px 8px rgba(0,0,0,0.07)", marginBottom: 20 }}>
              <div style={{ fontWeight: 800, color: LPU_NAVY, marginBottom: 14, fontSize: 15 }}>📊 SDG Score Comparison — All 17 Goals</div>
              <ResponsiveContainer width="100%" height={220}>
                <BarChart data={sdgs} margin={{ left: -20, right: 10 }}>
                  <XAxis dataKey="id" tick={{ fontSize: 11 }} />
                  <YAxis domain={[0, 100]} tick={{ fontSize: 11 }} />
                  <Tooltip formatter={(v, n, p) => [`${v}/100`, p.payload.name]} labelFormatter={(l) => `SDG ${l}`} />
                  <Bar dataKey="score" radius={[5, 5, 0, 0]}>
                    {sdgs.map((s) => <Cell key={s.id} fill={s.color} />)}
                  </Bar>
                </BarChart>
              </ResponsiveContainer>
            </div>
            <div style={{ background: "#fff", borderRadius: 14, padding: 20, boxShadow: "0 2px 8px rgba(0,0,0,0.07)" }}>
              <div style={{ fontWeight: 800, color: LPU_NAVY, marginBottom: 14, fontSize: 15 }}>🔝 Top 5 SDG Progress</div>
              {[...sdgs].sort((a, b) => b.score - a.score).slice(0, 5).map(s => (
                <div key={s.id} style={{ marginBottom: 14 }}>
                  <div style={{ display: "flex", justifyContent: "space-between", fontSize: 13, fontWeight: 600, color: LPU_NAVY, marginBottom: 4 }}>
                    <span>{s.icon} SDG {s.id} — {s.name}</span>
                    <span style={{ color: getScoreColor(s.score) }}>{s.score}%</span>
                  </div>
                  <div style={{ background: "#eee", borderRadius: 10, height: 10 }}>
                    <div style={{ width: `${s.score}%`, background: s.color, height: 10, borderRadius: 10 }} />
                  </div>
                </div>
              ))}
            </div>
          </>
        )}

        {activeTab === "goals" && (
          <div style={{ background: "#fff", borderRadius: 14, padding: 20, boxShadow: "0 2px 8px rgba(0,0,0,0.07)" }}>
            <div style={{ fontWeight: 800, color: LPU_NAVY, marginBottom: 16, fontSize: 15 }}>🎯 All 17 SDGs — India Progress</div>
            {sdgs.map(s => (
              <div key={s.id} style={{ marginBottom: 16 }}>
                <div style={{ display: "flex", justifyContent: "space-between", fontSize: 13, fontWeight: 600, color: LPU_NAVY, marginBottom: 5 }}>
                  <span>{s.icon} SDG {s.id} — {s.name}</span>
                  <span style={{ fontWeight: 800, color: getScoreColor(s.score) }}>{s.score}/100</span>
                </div>
                <div style={{ background: "#eee", borderRadius: 10, height: 11 }}>
                  <div style={{ width: `${s.score}%`, background: s.color, height: 11, borderRadius: 10 }} />
                </div>
                <div style={{ fontSize: 10, color: "#aaa", marginTop: 2 }}>
                  {s.score >= 65 ? "✅ On Track" : s.score >= 50 ? "⚠️ Needs Attention" : "❌ Critical — Action Required"}
                </div>
              </div>
            ))}
          </div>
        )}

        {activeTab === "timeline" && (
          <div style={{ background: "#fff", borderRadius: 14, padding: 20, boxShadow: "0 2px 8px rgba(0,0,0,0.07)" }}>
            <div style={{ fontWeight: 800, color: LPU_NAVY, marginBottom: 18, fontSize: 15 }}>📅 India SDG Milestones & Targets</div>
            <div style={{ position: "relative", paddingLeft: 28 }}>
              <div style={{ position: "absolute", left: 10, top: 0, bottom: 0, width: 2, background: `linear-gradient(to bottom, ${LPU_GOLD}, ${LPU_NAVY})`, borderRadius: 4 }} />
              {milestones.map((m, i) => (
                <div key={i} style={{ position: "relative", marginBottom: 22 }}>
                  <div style={{ position: "absolute", left: -22, top: 4, width: 14, height: 14, borderRadius: "50%", background: i === milestones.length - 1 ? LPU_NAVY : LPU_GOLD, border: "2px solid #fff" }} />
                  <div style={{ background: LPU_LIGHT, borderRadius: 10, padding: "10px 14px", borderLeft: `3px solid ${i === milestones.length - 1 ? LPU_NAVY : LPU_GOLD}` }}>
                    <div style={{ fontWeight: 700, fontSize: 12, color: i === milestones.length - 1 ? LPU_NAVY : LPU_GOLD }}>{m.date}</div>
                    <div style={{ fontSize: 13, color: "#333", marginTop: 2 }}>{m.event}</div>
                  </div>
                </div>
              ))}
            </div>
          </div>
        )}

        <div style={{ textAlign: "center", marginTop: 20, fontSize: 11, color: "#aaa" }}>
          Data based on India SDG Index estimates • LPU CHE110 — Environmental Studies • 2025
        </div>
      </div>
    </div>
  );
}
